/**
 * Audit utilities to decrypt and view private positions using viewing keys.
 */
export async function decryptPosition(encryptedData: string, viewingKey: string) {
  // TODO: implement position decryption
  return { amount: 0n, tokenId: 0n };
}
